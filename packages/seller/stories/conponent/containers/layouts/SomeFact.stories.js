import SomeFact from '../../../../components/containers/layouts/SomeFact'

export default{
    title:'components/containers/layouts/SomeFact',
    component:SomeFact,
}

const Template = (args) => <SomeFact/>
export const fact = Template.bind({})